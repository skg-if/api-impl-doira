/**
 * Constants defined by the SKG-IF OpenAPI specification itself (entity-type discriminator
 * values, per-entity filter-key vocabularies) - shared across providers so they never drift apart.
 */
package org.skgif.doi.spec;
